# ==================== 绝不入库：本机密钥与本地资料区 ====================
# docs/ 按 2026-09-24 用户裁定**整体不入库、不上传、不参与索引**：
#   - docs/simi/ 是仓库 deploy key 私钥明文，入库等于把写权限钥匙挂进同一仓库；
#   - docs/*.md（迁移方案/交接/剩余清单）含本机绝对路径与智能体工作姿势，
#     属环境强绑定的本地资料区，随仓库分发会误导其他机器（同源仓库 Trim 的
#     `update history/` 与 AGENTS.md 口径）。
# 注意：.gitignore 只拦未跟踪文件，历史上已提交过的必须另做剔除（见推送前门禁）。
docs/
# AGENTS.md：本机智能体协作约束，含绝对路径倾向与操作姿势，不随仓库分发
AGENTS.md
# assets-src/：图标/壁纸设计源文件（PSD/PNG 源稿），构建产物已在 src/assets/，源稿不入库
assets-src/
*.pem
*.key
*.pfx
*.p12

# ==================== 构建产物 ====================
# cargo 的编译缓存，体积随增量编译单调增长（本机量级到几十 GB 都正常），纯产物、永不入库
src-tauri/target/
# native-scanner 已 vendor 成独立（非 workspace）包，单独 cargo build 会生成不被上面规则覆盖的 target
native-scanner/target/
# tauri-build 构建期自动重新生成（acl-manifests / capabilities / *-schema.json）
src-tauri/gen/
node_modules/
dist/
build-release/

# ==================== 本机临时件与日志 ====================
*.log
tmp/
Thumbs.db
Desktop.ini
ehthumbs.db

# ==================== 本机智能体工作区 ====================
# 环境强绑定信息（绝对路径、本机操作姿势），随仓库分发会误导其他机器
.qoder/
.workbuddy/
.opencode/
