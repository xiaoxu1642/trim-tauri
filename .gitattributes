# 行尾唯一真源：仓库内一律 LF。
# 本机 core.autocrlf=true 来自 Git-for-Windows 系统级配置
# （C:/Program Files/Git/etc/gitconfig）。若不在此钉住，干净 clone 会把全部
# 源码转成 CRLF —— 而 tools/check-ps-extraction.mjs 与 check-ps-substitution.mjs
# 是拿 .ps1 与源仓库 JS 做**字节级**对拍的，行尾一变 PS 门禁必然整批红。
# .gitattributes 优先级高于任何 autocrlf 设置，故用它在仓库内自我修正。
* text=auto eol=lf

# ==================== 必须按字节原样保留的二进制 ====================
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.bmp binary
*.ico binary
*.icns binary
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary
*.zip binary
*.exe binary
*.dll binary
*.dylib binary
*.so binary

# ==================== .ps1 是「字节即内容」的例外，禁止任何行尾转换 ====================
# 迁移方案 D6 的门禁 tools/check-ps-extraction.mjs 是拿 src-tauri/ps/*.ps1 与源仓库
# Trim 的 JS 模板字面量做**逐字节**对拍的。上游这些字面量里嵌的 .reg 文本本身就带 \r\n
# （例如 maint_net_qos_scheduler / maint_tf_net_tcp / maint_net_disable_lmhosts 等 6 个
# 脚本里的 "Windows Registry Editor Version 5.00\r"），所以 \r 属于**内容**而不是行尾噪声。
#
# 曾经按「全仓 LF」把它们标成 text eol=lf，结果 6 项对拍全红、且这个回归被推到了远端
# —— 干净克隆出来的 .ps1 是 LF，与上游字面量差一个 \r。故这里显式关掉转换。
# 改这些文件只能通过 `node tools/sync-ps-from-js.mjs` 重出（AGENTS.md：禁止手改）。
# 另记一条容易记错的约束（审查 v2-L3 实测订正）：盘上 66 个 .ps1 **都不带 BOM**（0/66），
# UTF-8 BOM 是运行期由 `src-tauri/src/pwsh/mod.rs` 的前置逻辑加的（PowerShell 5.1 兼容）。
# `-text` 真正保住的是那 6 个嵌在 .reg 文本里的 `\r`——它属于内容，被归一成 LF 就整批对拍红。
*.ps1 -text

# ==================== 明确钉为 LF 的文本类型 ====================
*.rs text eol=lf
*.js text eol=lf
*.mjs text eol=lf
*.cjs text eol=lf
*.json text eol=lf
*.html text eol=lf
*.css text eol=lf
*.md text eol=lf
*.toml text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.lock text eol=lf
*.manifest text eol=lf

# ==================== 上游基线快照：字节即内容，禁止任何行尾转换（必须放在最后，后写覆盖先写） ====================
# `vendor/upstream-js/` 是从 Electron 轨（源仓库 Trim）逐字节复制进来的**只读基线**：
# `.ps1` 生成器与两套 PS 门禁会把它里面的模板字面量**按文本切片**再与 src-tauri/ps/*.ps1
# 逐字节对拍。源仓库这些文件是 CRLF，模板里的换行是真实换行——一旦被上面的
# `*.js text eol=lf` 归一成 LF，切出来的脚本就少掉 \r，对拍整批红（AGENTS.md §5.1 同一陷阱）。
# 要改这份快照只能整文件重新复制，禁止手工编辑（要改逻辑请改源仓库后重拷）。
vendor/upstream-js/** -text
